[package]
name = "devicemem_gpu"
version = "0.0.1"
authors = ["Peter Jin <peterhaijin@gmail.com>"]

[lib]
name = "devicemem_gpu"
path = "src/lib.rs"

[dependencies]
cuda = { path = "../libcuda" }
cuda_blas = { path = "../libcuda_blas" }
cuda_dnn = { path = "../libcuda_dnn" }
